# Leave empty for same-origin deployments that proxy /api to the backend.
# Set this when the backend is hosted on another origin, for example:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=https://synapse-um0x.onrender.com
